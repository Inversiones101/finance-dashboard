# Postgres de producción (Neon). Sin esta variable, en desarrollo se usa PGlite local.
DATABASE_URL=
# Conexión directa para migraciones (Neon la crea sola).
DATABASE_URL_UNPOOLED=
# Código que se pide al crear el primer admin en producción.
SETUP_TOKEN=
